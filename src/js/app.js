document.addEventListener('DOMContentLoaded', () => {
    startApp();
})

function startApp() {
    // selectAnimation();
    resetFileInput();
    dashboard();
    dragNdrop();
    // monthInput();
}